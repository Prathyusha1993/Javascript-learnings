let price = 30;
showMessage(price);
showMessage(typeof price); //tells the typeof price like number, string etc.. here it is num

let cost = 20;
cost  = cost + 34; //or cost += 30;
showMessage(cost);
console.log(cost);
cost  = cost -4;  //or cost -= 3;
showMessage(cost);
console.log(cost);
cost  = cost *3;   //or cost *= 10;
showMessage(cost);
console.log(cost);
cost  = cost / 5;      //or cost /= 3;
showMessage(cost);
console.log(cost);
cost  = cost % 3;     //or cost %= 3     //gives reminder, is also called as moduleous operator
showMessage(cost);
console.log(cost);
 
let value = 12;
showMessage(++value);
console.log(value);
showMessage(value++);
console.log(value);
showMessage(--value);
console.log(value);
showMessage(value--);
console.log(value);